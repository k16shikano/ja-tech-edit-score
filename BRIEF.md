# このプロジェクトの目的

一人の編集者の推敲履歴と、根拠を明示しない選好だけから、潜在的な推敲の質を言語モデルに埋め込むこと。
そのような言語モデルは、評価器および生成器として応用できることを前提とする。
対象の言語は日本語のみとする。

利用できる学習データは以下の通り。

- 学習用データA：下書きと「人間による推敲」のペアが 1976 件（すべて人間がレビュー済み）
    - `data/revision_corpus/canonical.jsonl`

- 学習用データA1：段落内の文章に対する推敲のみを集めたペアが 1597 件
    - 学習時に使うもの：`data/revision_corpus/keep_hunk_nopara.jsonl`
    - 検証用の取り分け： `data/edit_sft_hunk_nopara/heldout.jsonl`（210 件）

- 学習用データA2：段落をまたがる推敲を集めたペアが 379 件
    - 学習時に使うもの：`data/revision_corpus/keep_section.jsonl`
    - 検証用の取り分け： `data/edit_sft_section/heldout.jsonl`（50 件）

- 学習用データB：下書きと「人間による推敲」のペア、下書きと「Composerによる推敲」のペア、「人間による推敲」と「Composerによる推敲」のペアをまとめたものが 378 件（「A2の下書き」に対する「[Composerの推敲](data/section_middle/revisions.jsonl)」のうち、人間がレビューで不合格とした1件を除いた）
    - 学習時に使うもの：`data/section_middle/pref_train.jsonl`
    - 検証用の取り分け：`data/section_middle/pref_valid.jsonl`
    - 下書きと「Composerによる推敲」のペアは、「人間による推敲」を下書きから作ったのと同じ人間により、「Composerの推敲は下書きより劣化していない」ことを目視で確認している

- 評価用データC：A2から検証用に取り分けておいた50件と、A1から検証用に取り分けておいた210件からさらに乱択した10件について、「Qwen＋Aで学習したSFTしたアダプタ」により下書きを推敲させたものが 60 件（人間による優劣の評価済み）
    - 対象 60 件の下書きと人間の推敲：`data/blind_eval/items.jsonl`
    - 各下書きについてSFTアダプタが出した推敲 8 本：`outputs/edit-sft-eval-v3/adapter_samples.jsonl`
    - 「人間の推敲」と「上記の8本から暫定的な評価器で選んだ1本」とのペア：`data/blind_eval/pairs_gold_vs_adapter_selected.jsonl`
    - 上記のペアに対する人間の判定：`data/blind_eval/judgments_gold_vs_adapter_selected.jsonl`

各種のデータの作り方は [docs/DATA.md](docs/DATA.md)を参照。


## 1. 日本語の推敲を評価する評価器を作る

評価器は一般的な文章品質や、明示した「推敲の種類」の合成点を学ぶものではない。
つまり、下書き $x$ と候補 $y$ に対する得点を $s(x,y)$、下書きを基準にした差を $\Delta(x,y)=s(x,y)-s(x,x)$ と書いたときに、$\Delta > 0$ なら「候補がその人による推敲に近い」（良い）、$Δ \leq 0$ なら「遠い」（悪い）として扱う。

評価器に、「潜在的な推敲の質」がスカラーとして埋め込めるかどうかは、検証すべき仮説。
で、これはおそらく成り立っていない。
なぜなら、選好の循環がなかったとしても、候補となる修正はそもそも比較不可能なことが多いから（片方に致命的な欠落や書き足しがあったり、無修正があったりする）。


### pref-bt-keep-pairsplit

- 学習用データはA1
- 損失はBradley-Terryのペアワイズロス（下書きを基準に、人間の推敲の点 s_w と下書きの点 s_l を出して、softplus(-(s_w - s_l)) を最小化）
- 最適化は AdamW
- 学習率は 1e-2
- 80 epoch（最後の重みを残した）

下書きと人間の推敲のそれぞれを、日本語埋め込みモデル ruri で文書ベクトルにする。
下書きと候補の差、絶対差、コサイン類似度、文字数を標準化して線形層に通して点にする。
この線形層の重みのみを更新し、ruri の重みは更新しない。

**検証結果**

検証210件のうち、
- 下書きを人間の推敲より上位に置いた結果はなかった

### pref-sentseq-keep-pairsplit

- 学習用データはA2
- 損失はBradley-Terryのペアワイズロス（下書きを基準に、人間の推敲の点 s_w と下書きの点 s_l​ を出して、softplus(-(s_w - s_l)) を最小化）
- 最適化は AdamW
- 学習率は 3e-4
- 最大 40 epoch（人間の推敲の点が高い割合が最も大きい epoch の重みを残した）

下書きと人間の推敲のそれぞれを文に分け、日本語埋め込みモデル ruri で文ベクトルを出す。
文ベクトルの列を Transformer でベクトル化し、下書きと候補の差、絶対差、コサイン類似度、文字数を線形層に通して点にする。
この Transformer と線形層の重みのみを更新し、ruri の重みは更新しない。

**検証結果**

検証50件のうち、
- 下書きを人間の推敲より上位に置いた結果はなかった
- 4件でComposerによる推敲（Bからの転用）を人間の推敲より上位に置いた

### pref-sentseq-section-triples

- 学習用データはB
- 損失はBradley-Terryのペアワイズロス（Bの3種類のペアのそれぞれについて、勝った側の点 s_w と負けた側の点 s_l を出し、softplus(-(s_w - s_l)) を最小化）
- 最適化は AdamW
- 学習率は 3e-4
- 最大 40 epoch（検証で勝った側の点が高い割合が最も大きい epoch の重みを残した）

点の計算は pref-sentseq-keep-pairsplit と同じである。

**検証結果**

検証50件のうち、
- 下書きを人間の推敲より上位に置いた結果はなかった
- 3件でComposerによる推敲を人間の推敲より上位に置いた

**この評価器を2026-08-15時点での正式採用とする**
考察は docs/why-pref-sentseq-section-triples.md を参照。

### pref-setwise-section-triples

- 学習用データはB
- 損失は ListMLE（人間の推敲、Composer、下書きの三点を、この順になるようにする）
- 最適化は AdamW
- 学習率は 3e-4
- 最大 40 epoch（検証で人間の推敲が最上だった割合が最も大きい epoch の重みを残した）

下書き、人間の推敲、Composer をそれぞれ文に分け、日本語埋め込みモデル ruri で文ベクトルを出す。
文章ごとに Transformer を通し、続けて三点を一つの Transformer に通して、候補のベクトルを線形層に通して点にする。
三点は一度の計算に入れる。
この Transformer と線形層の重みのみを更新し、ruri の重みは更新しない。

**検証結果**

検証50件のうち、
- 9件で下書きを人間の推敲より上位に置いた
- 14件でComposerによる推敲を人間の推敲より上位に置いた

### pref-setwise-section-human-top

- 学習用データはB
- 損失は logsumexp(三点の点) − 人間の推敲の点（Composer と下書きの上下は損失に入れない）
- 最適化は AdamW
- 学習率は 3e-4
- 最大 40 epoch（検証で人間の推敲が最上だった割合が最も大きい epoch の重みを残した）

点の計算は pref-setwise-section-triples と同じである。

**検証結果**

検証50件のうち、
- 15件で下書きを人間の推敲より上位に置いた
- 19件でComposerによる推敲を人間の推敲より上位に置いた

### pref-pair-draft

- 学習用データはBのうち、下書きと人間の推敲
- 損失は logsumexp(二点の点) − 人間の推敲の点
- 最適化は AdamW
- 学習率は 3e-4
- 最大 40 epoch（検証で人間の推敲が最上だった割合が最も大きい epoch の重みを残した）

点の計算に使う部品は pref-setwise-section-triples と同じだが、入力は下書きと候補の一方だけである。

**検証結果**

検証50件のうち、学習の乱数 `--seed` を 0、1、2 とした3回では、
- 6件から9件で下書きを人間の推敲より上位に置いた
- 12件から15件でComposerによる推敲を人間の推敲より上位に置いた

### pref-pair-humantop

- 学習用データはB
- 損失は logsumexp(三点の点) − 人間の推敲の点（Composer と下書きの上下は損失に入れない）
- 最適化は AdamW
- 学習率は 3e-4
- 最大 40 epoch（検証で人間の推敲が最上だった割合が最も大きい epoch の重みを残した）

点の計算は pref-pair-draft と同じである。入力は下書きと候補の一方だけである。

**検証結果**

検証50件のうち、学習の乱数 `--seed` を 0、1、2 とした3回では、
- 6件から12件で下書きを人間の推敲より上位に置いた
- 10件から15件でComposerによる推敲を人間の推敲より上位に置いた

### pref-pair-humantop-hunk-then-section

- 学習用データは、先に A1、続けて B
- 損失は logsumexp − 人間の推敲の点（A1 では二点、B では三点。Composer と下書きの上下は損失に入れない）
- 最適化は AdamW
- 学習率は 3e-4
- A1 を 10 epoch したあと、B を最大 40 epoch（検証で人間の推敲が最上だった割合が最も大きい epoch の重みを残した）

点の計算は pref-pair-draft と同じである。

**検証結果**

検証50件のうち、学習の乱数 `--seed` を 0、1、2 とした3回では、
- 10件から15件で下書きを人間の推敲より上位に置いた
- 12件から18件でComposerによる推敲を人間の推敲より上位に置いた

### pref-pair-humantop-hunk-replay

- 学習用データは、先に A1、続けて A1 と B を混ぜたもの
- 損失は logsumexp − 人間の推敲の点（Composer と下書きの上下は損失に入れない）
- 最適化は AdamW
- 学習率は 3e-4
- A1 を 10 epoch したあと、A1 と B を混ぜて最大 40 epoch（検証で人間の推敲が最上だった割合が最も大きい epoch の重みを残した）

点の計算は pref-pair-draft と同じである。

**検証結果**

検証50件のうち、学習の乱数 `--seed` を 0、1、2 とした3回では、
- 8件から14件で下書きを人間の推敲より上位に置いた
- 10件から16件でComposerによる推敲を人間の推敲より上位に置いた

### pref-joint-humantop-hunk-replay

- 学習用データは pref-pair-humantop-hunk-replay と同じ
- 損失は logsumexp(三点の点) − 人間の推敲の点（Composer と下書きの上下は損失に入れない）
- 最適化は AdamW
- 学習率は 3e-4
- A1 を 10 epoch したあと、A1 と B を混ぜて最大 40 epoch（検証で人間の推敲が最上だった割合が最も大きい epoch の重みを残した）

点の計算は pref-setwise-section-triples と同じである。三点は一度の計算に入れる。

**検証結果**

検証50件のうち、学習の乱数 `--seed` を 0、1、2 とした3回では、
- 10件から14件で下書きを人間の推敲より上位に置いた
- 10件から15件でComposerによる推敲を人間の推敲より上位に置いた

### pref-nce-section

- 学習用データはB
- 損失は InfoNCE（正例は人間の推敲、負例は下書きと Composer の推敲。Composer と下書きの上下は損失に入れない）
- 最適化は AdamW
- 学習率は 3e-4
- 温度は 0.07
- 最大 40 epoch（学習側の InfoNCE が最小だった 37 epoch の重みを残した）

下書き、人間の推敲、Composer をそれぞれ文に分け、日本語埋め込みモデル ruri で文ベクトルを出す。
文ベクトルの列を Transformer で文書ベクトルにする。
下書きの文書ベクトルを 2 層の写像でクエリ q にし、候補の文書ベクトル v とのコサインを点にする。
この Transformer とクエリの写像の重みのみを更新し、ruri の重みは更新しない。

**検証結果**

検証50件のうち、
- 6件で下書きを人間の推敲より上位に置いた
- 12件でComposerによる推敲を人間の推敲より上位に置いた

### pref-detect-section

- 学習用データはB
- 損失は二値交差エントロピー（人間の推敲を 1、下書きと Composer の推敲を 0 とする。Composer 対下書きの対は入れない）
- 最適化は AdamW
- 学習率は 3e-4
- 最大 40 epoch（学習側の二値交差エントロピーが最小だった 40 epoch 目の重みを残した）

点の計算は pref-sentseq-keep-pairsplit と同じである。下書きと候補のそれぞれを文に分け、日本語埋め込みモデル ruri で文ベクトルを出す。
文ベクトルの列を Transformer でベクトル化し、下書きと候補の差、絶対差、コサイン類似度、文字数を線形層に通して点にする。
この Transformer と線形層の重みのみを更新し、ruri の重みは更新しない。

**検証結果**

検証50件のうち、
- 2件で下書きを人間の推敲より上位に置いた
- 5件でComposerによる推敲を人間の推敲より上位に置いた

### pref-detect-cd-section

- 学習用データはB
- 損失は二値交差エントロピー（人間の推敲を 1、下書きと Composer の推敲を 0 とする）に、Composer の点が下書きより高くなる項を足す
- 最適化は AdamW
- 学習率は 3e-4
- 最大 40 epoch（学習側の合計損失が最小だった 36 epoch 目の重みを残した）

点の計算は pref-detect-section と同じである。

**検証結果**

検証50件のうち、
- 0件で下書きを人間の推敲より上位に置いた
- 6件でComposerによる推敲を人間の推敲より上位に置いた


### 評価器の解釈

LLMによる推敲は、たまたま「人間の推敲」に近いこともある。
実際、60件テストでは、推敲した人間本人が判定しても8割程度しか「人間の推敲である」ことを当てられていない。

したがって、人間の推敲＞Composerの推敲を100%当てることは要件ではない。
「人間の推敲 vs Composerの推敲」を人間自身（ブラインド）と評価器がそれぞれ独立して評価し、それらの結果がどれくらい一致するかを確かめるべき。

対象は学習用データBの検証50件である。左右に人間の推敲とComposerの推敲を並べ、役割は見せない。下書きは文脈として出す。

判定の結果、人が同等としたのは3件である。人間の推敲のほうを選んだのは43件、Composerの推敲のほうを選んだのは4件である。






